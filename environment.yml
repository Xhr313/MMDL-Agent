name: MMDL-Agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - fastapi>=0.115
    - uvicorn>=0.30
    - pydantic>=2.7
    - pydantic-settings>=2.3
    - langchain>=0.2
    - langgraph>=0.2
    - httpx>=0.27
    - python-dotenv>=1.0
    - orjson>=3.10
    - pytest>=8.2
    - ruff>=0.5
    - mypy>=1.10
    - pytest-asyncio
    - pytest-cov
    - pytest-xdist
    - -e .
